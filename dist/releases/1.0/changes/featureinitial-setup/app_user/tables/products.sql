-- liquibase formatted sql
-- changeset APP_USER:1755218780355 stripComments:false  logicalFilePath:featureinitial-setup\app_user\tables\products.sql runAlways:false runOnChange:false replaceIfExists:true failOnError:true
-- sqlcl_snapshot src/database/app_user/tables/products.sql:null:15be775eb0eb1bcbdbd250cb03274d9fed79697e:create

create table products (
    product_id     number
        generated by default on null as identity
    not null enable,
    product_name   varchar2(100 byte) not null enable,
    category       varchar2(50 byte),
    price          number(10, 2),
    stock_quantity number(10, 0)
);

alter table products
    add constraint pk_product primary key ( product_id )
        using index enable;

